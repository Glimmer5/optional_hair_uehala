import { VStack } from "@chakra-ui/react";

const Home: React.FC = () => {
  return (
    <VStack alignItems={{ sm: "center", md: "center", lg: "start" }}>
    </VStack>
  );
};

export default Home;
